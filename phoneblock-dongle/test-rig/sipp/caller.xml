<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE scenario SYSTEM "sipp.dtd">

<!--
  Spam-caller scenario: send an INVITE directly to the dongle's
  SIP listening port (default UDP 5061), play through the call,
  wait for the dongle's BYE.

  We bypass the registrar lookup on purpose — the dongle accepts
  INVITEs on its bound socket regardless of source. That keeps this
  scenario self-contained: no need for SIPp to share state with the
  registrar instance.

  Usage (set the dongle IP via -rsa):

    sipp -sf caller.xml -m 1 -s +4915112345678 \
         -rsa <DONGLE_IP>:5061 -i <YOUR_HOST_IP>

  -s sets the [service] placeholder used as the To-User; pick a
  number that the PhoneBlock /api/check-prefix endpoint will rate
  as spam (or set SIP_TEST_FORCE_SPAM_STAR_NUMBERS during build to
  force-spam any number with a leading "*").
-->
<scenario name="PhoneBlock Dongle Spam Caller">

  <send retrans="500">
    <![CDATA[

      INVITE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Spam Caller" <sip:[service]@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>
      Call-ID: [call_id]
      CSeq: 1 INVITE
      Contact: <sip:caller@[local_ip]:[local_port]>
      Max-Forwards: 70
      User-Agent: SIPp Spam Caller (PhoneBlock dongle test-rig)
      Subject: Spam call test
      Content-Type: application/sdp
      Content-Length: [len]

      v=0
      o=caller 53655765 2353687637 IN IP[local_ip_type] [local_ip]
      s=Spam call test
      c=IN IP[media_ip_type] [media_ip]
      t=0 0
      m=audio [media_port] RTP/AVP 8 101
      a=rtpmap:8 PCMA/8000
      a=rtpmap:101 telephone-event/8000
      a=fmtp:101 0-15
      a=sendrecv

    ]]>
  </send>

  <recv response="100" optional="true"/>
  <recv response="180" optional="true"/>
  <recv response="183" optional="true"/>
  <recv response="200" rtd="true"/>

  <send>
    <![CDATA[

      ACK sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Spam Caller" <sip:[service]@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 1 ACK
      Contact: <sip:caller@[local_ip]:[local_port]>
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <!--
    The dongle plays its announcement, then sends BYE.
    We give it 60 s; long enough for the full announcement plus
    PhoneBlock /api/check-prefix roundtrip.
  -->
  <recv request="BYE" timeout="60000" ontimeout_next="hangup"/>

  <send>
    <![CDATA[

      SIP/2.0 200 OK
      [last_Via:]
      [last_From:]
      [last_To:]
      [last_Call-ID:]
      [last_CSeq:]
      Content-Length: 0

    ]]>
  </send>

  <label id="end"/>
  <ResponseTimeRepartition value="500, 1000, 2000, 5000, 10000, 30000"/>

  <!-- Fallback: dongle didn't BYE in time, hang up ourselves -->
  <label id="hangup"/>
  <send>
    <![CDATA[

      BYE sip:[service]@[remote_ip]:[remote_port] SIP/2.0
      Via: SIP/2.0/[transport] [local_ip]:[local_port];branch=[branch]
      From: "Spam Caller" <sip:[service]@[local_ip]:[local_port]>;tag=[pid]SIPpTag00[call_number]
      To: <sip:[service]@[remote_ip]:[remote_port]>[peer_tag_param]
      Call-ID: [call_id]
      CSeq: 2 BYE
      Max-Forwards: 70
      Content-Length: 0

    ]]>
  </send>

  <recv response="200" optional="true"/>

</scenario>
