# PhoneBlock Dongle 1.4.1

*2026-07-06*

## Added

- **E-mail when the firmware updates itself.** After the dongle installs a
  new firmware version and restarts into it, it now sends a short e-mail —
  "Die Firmware auf deinem PhoneBlock-Dongle wurde auf Version X.Y.Z
  aktualisiert." — with the version linked to that release's changelog and
  the device name linked to its own web interface. So you find out what
  changed without having to check the dongle yourself. It goes through the
  same "Status e-mail" account you configured and, unlike the error and
  spam notifications, is on by default; a checkbox switches it off.

## Changed

- **The status e-mail now lists every new call, not only blocked spam.**
  The daily mail used to be sent only when a spam call had been blocked. It
  now reports on all recorded calls — including the ones passed through —
  so the mail is a complete picture of what the dongle handled.

## Fixed

- **The status e-mail no longer repeats calls from previous days.** Each
  daily mail used to print the entire recent-calls list, so calls already
  reported the day before showed up again. The mail now lists only the
  calls that arrived since the last one was sent.

- **No more spurious "STUN gave no mapping" warning on a Fritz!Box.** The
  1.4.0 STUN feature is only meant for registering straight at a provider
  behind a home router; on a Fritz!Box the dongle registers on the local
  network and its media reaches the box directly, so there is no NAT to
  look up. The dongle nevertheless attempted a STUN query on every
  incoming call and, finding no STUN server configured, logged an alarming
  "STUN gave no mapping — advertising local RTP endpoint …" warning to the
  diagnostics panel — even though the local endpoint it announced was
  exactly correct. The dongle now skips STUN entirely when no server is
  configured (the Fritz!Box default) and announces the local endpoint
  quietly, so the panel no longer shows a fault where there is none.
